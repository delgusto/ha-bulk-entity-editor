// Injected by Vite's `define` from vite.config.ts. Values match the
// integration's manifest.json version and the build timestamp.
declare const __BUILD_VERSION__: string;
declare const __BUILD_TIME__: string;
